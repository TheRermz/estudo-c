#include <stdio.h> //allows to use fuctions from this library

int main()
{
    printf("olar\n");
    int x = 50;
    printf("%s World\n", "Hello");
    printf("The value of x is %d\n", x);
    printf("Give me a radius:");
    int radius;
    scanf("%d", &radius); // address of operator (pointer)
    printf("You chose dea... the value %d\n", radius);
    return 0;
}
